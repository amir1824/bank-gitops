apiVersion: v1
kind: Service
metadata:
  name: {{ include "mainframe-mock.fullname" . }}
  labels:
    {{- include "mainframe-mock.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mainframe-mock.selectorLabels" . | nindent 4 }}
